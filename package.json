{
  "name": "predguard",
  "version": "1.0.0",
  "private": true,
  "description": "PredatorGuard prototype with a local Node auth backend.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "supabase:push": "supabase db push",
    "supabase:new": "supabase migration new"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.0",
    "@supabase/supabase-js": "^2.108.2"
  }
}
